name: Build (Tauri)

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
      - master

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-24.04, windows-latest]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust build
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('src-tauri/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Install Linux dependencies
        if: matrix.os == 'ubuntu-24.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            pkg-config \
            libgtk-3-dev \
            libwebkit2gtk-4.1-dev \
            libjavascriptcoregtk-4.1-dev \
            libsoup-3.0-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            libssl-dev \
            rpm

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

<<<<<<< Updated upstream
      - name: List build output
        run: |
          echo "=== Listing target/release/bundle ==="
          ls -laR target/release/bundle/ || echo "Directory not found"

      - name: Upload bundles
        uses: actions/upload-artifact@v4
        with:
          name: keyboarder-${{ matrix.os }}
          path: target/release/bundle/**
          if-no-files-found: error
=======
<<<<<<< Updated upstream
      - name: Upload bundles
        uses: actions/upload-artifact@v4
        with:
          name: key-label-layout-${{ matrix.os }}
          path: src-tauri/target/release/bundle/**
=======
      - name: List build output
        run: |
          echo "=== Finding all bundle directories ==="
          find . -type d -name "bundle" 2>/dev/null || true
          echo ""
          echo "=== Finding all .deb, .rpm, .AppImage files ==="
          find . -type f \( -name "*.deb" -o -name "*.rpm" -o -name "*.AppImage" -o -name "*.msi" -o -name "*.exe" \) 2>/dev/null || true
          echo ""
          echo "=== Listing target/ root ==="
          ls -la target/ 2>/dev/null || echo "target/ not found"
          echo ""
          echo "=== Listing src-tauri/target/release/ ==="
          ls -la src-tauri/target/release/ 2>/dev/null || echo "src-tauri/target/release/ not found"

      - name: Upload bundles
        uses: actions/upload-artifact@v4
        with:
          name: keyboarder-${{ matrix.os }}
          path: target/release/bundle/**
          if-no-files-found: warn
>>>>>>> Stashed changes
>>>>>>> Stashed changes
